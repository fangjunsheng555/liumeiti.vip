export const metadata = {
  title: "Netflix 登录码",
  description: "确认订单后，获取 Netflix 4 位登录码或前往 Netflix 完成登录确认。",
  robots: { index: false, follow: false, nocache: true },
};

export default function NetflixCodeLayout({ children }) {
  return children;
}
